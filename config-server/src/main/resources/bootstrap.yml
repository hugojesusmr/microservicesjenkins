server:
    port: 8000
  
spring:
  cloud:
    config:
      server: 
        git:
          uri: https://github.com/hugojesusmr/MicroServiceEcommerce.git
          default-label: master
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
       
          
